# Stitches

### Stitches styled components have inferred prop types.

```tsx
const Foo = styled("bar", {
  variants: {
    intent: {
      demo: {
        color: "red"
      }
    }
  }
})
```
